import * as React from 'react';
import { Breadcrumb, Button, Col, Form, Image, Input, Row, Spin, Upload } from 'antd';
import { RollbackOutlined } from '@ant-design/icons';
import { useState } from 'react';
import FormWork from '@/components/work/form-work';
export interface WorkUpdateProps {
}

export default function WorkUpdate(props: WorkUpdateProps) {
  
  return (
    <>
      <FormWork></FormWork>
    </>
  );
}
